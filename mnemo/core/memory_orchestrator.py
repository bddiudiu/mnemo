"""Memory Orchestrator: coordinates all three memory layers.

Routes memory operations to the appropriate layer(s) based on
memory_type and query characteristics.
"""

from __future__ import annotations

# Placeholder — full implementation in Week 1-2.
# The orchestrator will:
# 1. Route store() calls to Working / Episodic / Semantic
# 2. Coordinate multi-layer recall() with merge + rank
# 3. Manage memory lifecycle (TTL expiry, eviction)
